# PolloAI - AI 创意内容生成平台 项目计划

> **参考产品**: [Pollo.ai](https://pollo.ai/zh)
> **核心 API**: Gemini Veo 3.1 (视频生成) + Nano Banana (图片生成)
> **文档版本**: v1.0 | **日期**: 2026-03-09

---

## 一、项目概述

### 1.1 产品定位

构建一个 AI 驱动的创意内容生成平台，提供**文字生图**和**图生视频**两大核心能力。用户通过简洁的 Web 界面输入文本描述或上传图片，即可生成高质量的 AI 图片和视频内容。

### 1.2 核心功能

| 功能模块 | 描述 | 底层 API |
|---------|------|---------|
| **文字生图** | 用户输入文本 Prompt，生成高质量 AI 图片 | Nano Banana (Gemini 系列图片模型) |
| **图生视频** | 用户上传图片，AI 生成动态视频 | Veo 3.1 (Gemini 视频生成模型) |

### 1.3 目标用户

- 内容创作者 / 自媒体运营
- 营销人员 / 广告设计师
- 独立开发者 / 小型团队
- 对 AI 生成内容感兴趣的普通用户

---

## 二、技术架构

### 2.1 整体架构图

```
┌─────────────────────────────────────────────────────────┐
│                    客户端 (Frontend)                      │
│              Next.js 14+ / React 18 / TailwindCSS        │
│         ┌──────────┬──────────┬───────────────┐          │
│         │ 文字生图  │ 图生视频  │ 任务管理/历史  │          │
│         └──────────┴──────────┴───────────────┘          │
└────────────────────────┬────────────────────────────────┘
                         │ HTTPS / WebSocket
┌────────────────────────▼────────────────────────────────┐
│                   API 网关 (Gateway)                      │
│           Nginx / Cloudflare / Vercel Edge               │
│        ┌──────────────────────────────────┐              │
│        │  限流 · 鉴权 · 日志 · CORS       │              │
│        └──────────────────────────────────┘              │
└────────────────────────┬────────────────────────────────┘
                         │
┌────────────────────────▼────────────────────────────────┐
│                  后端服务 (Backend)                       │
│              Node.js (NestJS) / Python (FastAPI)         │
│  ┌───────────┬────────────┬──────────┬───────────┐      │
│  │ 用户服务   │ 任务调度    │ 积分系统  │ 文件服务   │      │
│  │ Auth/User │ Task Queue │ Credits  │ Storage   │      │
│  └───────────┴────────────┴──────────┴───────────┘      │
└──────────┬─────────────────────────────┬────────────────┘
           │                             │
┌──────────▼──────────┐    ┌─────────────▼───────────────┐
│   AI 服务适配层       │    │       数据层                 │
│  ┌────────────────┐  │    │  ┌──────────┬────────────┐  │
│  │ Nano Banana    │  │    │  │ PostgreSQL│ Redis     │  │
│  │ 图片生成适配器  │  │    │  │ 业务数据   │ 缓存/队列  │  │
│  ├────────────────┤  │    │  ├──────────┼────────────┤  │
│  │ Veo 3.1        │  │    │  │ S3/OSS   │ CDN       │  │
│  │ 视频生成适配器  │  │    │  │ 文件存储   │ 内容分发   │  │
│  └────────────────┘  │    │  └──────────┴────────────┘  │
└──────────┬───────────┘    └─────────────────────────────┘
           │
┌──────────▼───────────┐
│   Google Gemini API   │
│  • Nano Banana Models │
│  • Veo 3.1 Models     │
└───────────────────────┘
```

### 2.2 技术栈选型

#### 前端

| 技术 | 版本 | 用途 |
|------|------|------|
| **Next.js** | 14+ (App Router) | 全栈框架、SSR/SSG、API Routes |
| **React** | 18+ | UI 组件库 |
| **TailwindCSS** | 4.x | 样式框架 |
| **shadcn/ui** | latest | UI 组件库 |
| **Zustand** | 5.x | 轻量状态管理 |
| **TanStack Query** | 5.x | 服务端状态 / 异步请求管理 |
| **Framer Motion** | latest | 动画效果 |

#### 后端

| 技术 | 版本 | 用途 |
|------|------|------|
| **Next.js API Routes** | 14+ | 轻量 API 层 (MVP 阶段) |
| **Python FastAPI** | 0.110+ | AI 服务适配层 (可选拆分) |
| **BullMQ** | 5.x | 任务队列 (视频生成异步处理) |
| **Redis** | 7.x | 缓存 / 队列 / 限流 |
| **PostgreSQL** | 16+ | 主数据库 |
| **Prisma** | 6.x | ORM |

#### AI API

| API | 模型 | 用途 | 备注 |
|-----|------|------|------|
| **Nano Banana** | `gemini-2.5-flash-image` | 文字生图 (高速) | 免费额度 500 次/天 |
| **Nano Banana Pro** | `gemini-3-pro-image-preview` | 文字生图 (高质量) | 需付费 |
| **Nano Banana 2** | `gemini-3.1-flash-image-preview` | 文字生图 (均衡) | 4K 输出 |
| **Veo 3.1** | `veo-3.1-generate-preview` | 图生视频 (高质量) | 720p/1080p, 4-8秒 |
| **Veo 3.1 Fast** | `veo-3.1-fast-generate-preview` | 图生视频 (快速) | 低延迟 |

#### 基础设施

| 技术 | 用途 |
|------|------|
| **Vercel** | 前端 + API Routes 部署 |
| **Docker + Railway/Fly.io** | Python AI 服务部署 |
| **Cloudflare R2 / AWS S3** | 文件存储 (图片/视频) |
| **Cloudflare CDN** | 静态资源 & 媒体分发 |
| **Upstash Redis** | Serverless Redis |
| **Neon / Supabase** | Serverless PostgreSQL |

---

## 三、功能模块详细设计

### 3.1 文字生图模块

#### 用户流程

```
用户输入 Prompt ──▶ 选择模型/参数 ──▶ 提交生成请求
       │
       ▼
  [后端] 参数校验 ──▶ 积分扣减 ──▶ 调用 Nano Banana API
       │
       ▼
  接收生成结果 ──▶ 存储到 S3/OSS ──▶ 返回图片 URL
       │
       ▼
  前端展示结果 ──▶ 支持下载/编辑/生成视频
```

#### API 调用示例 (Nano Banana)

```python
from google import genai
from google.genai import types

client = genai.Client(api_key="YOUR_API_KEY")

# 文字生图
response = client.models.generate_content(
    model="gemini-2.5-flash-image",  # 或 gemini-3-pro-image-preview
    contents=["一只柴犬在樱花树下奔跑，电影级画质，黄金时刻光线"],
    config=types.GenerateContentConfig(
        response_modalities=["IMAGE"],
    ),
)

# 提取图片数据
for part in response.candidates[0].content.parts:
    if part.inline_data is not None:
        image_data = part.inline_data.data  # bytes
        # 上传到 S3/OSS
```

#### 可配置参数

| 参数 | 说明 | 默认值 |
|------|------|--------|
| 模型选择 | Flash (快) / Pro (精) / Flash 2 (均衡) | Flash |
| 图片尺寸 | 1024×1024 / 1024×1792 / 1792×1024 | 1024×1024 |
| 生成数量 | 1-4 张 | 1 |
| 风格预设 | 写实 / 动漫 / 插画 / 3D / 油画 等 | 无 |
| 负面提示词 | 排除不需要的元素 | 空 |

### 3.2 图生视频模块

#### 用户流程

```
上传/选择图片 ──▶ 输入动态描述 ──▶ 选择参数 ──▶ 提交生成
       │
       ▼
  [后端] 参数校验 ──▶ 积分扣减 ──▶ 创建异步任务
       │
       ▼
  [任务队列] 调用 Veo 3.1 API ──▶ 轮询等待结果
       │
       ▼
  生成完成 ──▶ 下载视频 ──▶ 存储到 S3/OSS
       │
       ▼
  [WebSocket] 通知前端 ──▶ 展示视频 ──▶ 支持下载/分享
```

#### API 调用示例 (Veo 3.1)

```python
import time
from google import genai
from google.genai import types

client = genai.Client(api_key="YOUR_API_KEY")

# 图生视频
operation = client.models.generate_videos(
    model="veo-3.1-generate-preview",
    prompt="镜头缓缓推进，柴犬抬头望向飘落的樱花瓣，浅景深效果",
    image=uploaded_image,  # PIL.Image 或 API 返回的 Image 对象
    config=types.GenerateVideosConfig(
        person_generation="allow_all",
        aspect_ratio="16:9",       # 或 "9:16" 竖屏
        number_of_videos=1,
    ),
)

# 异步轮询等待结果
while not operation.done:
    time.sleep(10)
    operation = client.operations.get(operation)

# 获取视频
for video in operation.result.generated_videos:
    video_data = client.files.download(file=video.video)
    # 上传到 S3/OSS
```

#### 可配置参数

| 参数 | 说明 | 选项 |
|------|------|------|
| 模型选择 | 标准 (质量优先) / 快速 (速度优先) | Veo 3.1 / Veo 3.1 Fast |
| 画面比例 | 横屏 / 竖屏 | 16:9 / 9:16 |
| 视频时长 | 4 / 6 / 8 秒 | 默认 8 秒 |
| 分辨率 | 标清 / 高清 | 720p / 1080p |
| 运动描述 | 镜头运动文字说明 | 自由输入 |
| 音频生成 | 是否生成原生音频 | 开/关 |

### 3.3 用户系统

| 功能 | 说明 |
|------|------|
| 注册/登录 | 邮箱 + OAuth (Google/GitHub) |
| 积分系统 | 注册赠送体验积分，消耗制 |
| 历史记录 | 生成历史浏览、重新生成、删除 |
| 个人设置 | 默认参数、通知偏好 |

### 3.4 积分 & 计费系统

| 操作 | 积分消耗 | 说明 |
|------|---------|------|
| 文字生图 (Flash) | 2 积分 | 快速模型，成本低 |
| 文字生图 (Pro) | 5 积分 | 高质量模型 |
| 图生视频 (Fast) | 15 积分 | 快速视频 |
| 图生视频 (标准) | 30 积分 | 高质量视频 |

---

## 四、数据库设计

### 4.1 核心表结构

```sql
-- 用户表
CREATE TABLE users (
    id            UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    email         VARCHAR(255) UNIQUE NOT NULL,
    name          VARCHAR(100),
    avatar_url    TEXT,
    password_hash TEXT,               -- OAuth 用户可为空
    provider      VARCHAR(20),        -- 'email' | 'google' | 'github'
    provider_id   VARCHAR(255),
    credits       INTEGER DEFAULT 10, -- 初始赠送积分
    role          VARCHAR(20) DEFAULT 'user',
    created_at    TIMESTAMPTZ DEFAULT NOW(),
    updated_at    TIMESTAMPTZ DEFAULT NOW()
);

-- 生成任务表
CREATE TABLE generation_tasks (
    id            UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id       UUID REFERENCES users(id) ON DELETE CASCADE,
    type          VARCHAR(20) NOT NULL,  -- 'text_to_image' | 'image_to_video'
    status        VARCHAR(20) DEFAULT 'pending',
    -- 'pending' | 'processing' | 'completed' | 'failed'
    prompt        TEXT,
    model         VARCHAR(100),          -- 使用的 AI 模型
    config        JSONB,                 -- 生成参数 (尺寸/时长/风格等)
    input_url     TEXT,                  -- 输入图片 URL (图生视频)
    output_urls   JSONB,                 -- 输出文件 URL 列表
    credits_cost  INTEGER NOT NULL,
    error_message TEXT,
    api_operation_id VARCHAR(255),       -- Gemini API 操作 ID
    started_at    TIMESTAMPTZ,
    completed_at  TIMESTAMPTZ,
    created_at    TIMESTAMPTZ DEFAULT NOW()
);

-- 积分交易记录
CREATE TABLE credit_transactions (
    id            UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id       UUID REFERENCES users(id) ON DELETE CASCADE,
    amount        INTEGER NOT NULL,      -- 正数=充值, 负数=消费
    type          VARCHAR(30) NOT NULL,
    -- 'signup_bonus' | 'purchase' | 'generation' | 'refund'
    reference_id  UUID,                  -- 关联任务/订单 ID
    balance_after INTEGER NOT NULL,
    description   TEXT,
    created_at    TIMESTAMPTZ DEFAULT NOW()
);

-- 索引
CREATE INDEX idx_tasks_user_id ON generation_tasks(user_id);
CREATE INDEX idx_tasks_status ON generation_tasks(status);
CREATE INDEX idx_tasks_created_at ON generation_tasks(created_at DESC);
CREATE INDEX idx_credits_user_id ON credit_transactions(user_id);
```

---

## 五、API 接口设计

### 5.1 接口列表

#### 认证模块

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/auth/register` | 邮箱注册 |
| POST | `/api/auth/login` | 邮箱登录 |
| GET  | `/api/auth/oauth/:provider` | OAuth 登录跳转 |
| GET  | `/api/auth/me` | 获取当前用户信息 |

#### 生成模块

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/generate/image` | 文字生图 |
| POST | `/api/generate/video` | 图生视频 |
| GET  | `/api/tasks/:id` | 查询任务状态 |
| GET  | `/api/tasks` | 任务历史列表 |
| DELETE | `/api/tasks/:id` | 删除任务记录 |

#### 文件模块

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/upload` | 上传图片 |
| GET  | `/api/files/:id` | 获取文件 |

#### 用户模块

| 方法 | 路径 | 说明 |
|------|------|------|
| GET  | `/api/user/credits` | 查询积分余额 |
| GET  | `/api/user/credits/history` | 积分交易记录 |

### 5.2 关键接口示例

#### 文字生图请求

```json
POST /api/generate/image
{
    "prompt": "一只柴犬在樱花树下奔跑，电影级画质",
    "model": "flash",           // "flash" | "pro" | "flash2"
    "size": "1024x1024",        // "1024x1024" | "1024x1792" | "1792x1024"
    "count": 1,                 // 1-4
    "style": "realistic",       // 可选风格预设
    "negative_prompt": ""       // 可选负面提示词
}

// 响应
{
    "task_id": "uuid",
    "status": "completed",
    "images": [
        {
            "url": "https://cdn.example.com/images/xxx.png",
            "width": 1024,
            "height": 1024
        }
    ],
    "credits_used": 2,
    "credits_remaining": 48
}
```

#### 图生视频请求

```json
POST /api/generate/video
{
    "image_url": "https://cdn.example.com/images/xxx.png",
    "prompt": "镜头缓缓推进，柴犬抬头望向飘落的樱花瓣",
    "model": "standard",        // "standard" | "fast"
    "aspect_ratio": "16:9",     // "16:9" | "9:16"
    "duration": 8,              // 4 | 6 | 8 (秒)
    "resolution": "1080p"       // "720p" | "1080p"
}

// 响应 (异步任务)
{
    "task_id": "uuid",
    "status": "processing",
    "estimated_time": 60,       // 预计秒数
    "credits_used": 30,
    "credits_remaining": 18
}
```

#### WebSocket 任务状态推送

```json
// 连接: ws://api.example.com/ws?token=xxx

// 服务端推送
{
    "event": "task_update",
    "data": {
        "task_id": "uuid",
        "status": "completed",
        "progress": 100,
        "output": {
            "video_url": "https://cdn.example.com/videos/xxx.mp4",
            "duration": 8,
            "resolution": "1080p"
        }
    }
}
```

---

## 六、前端页面设计

### 6.1 页面结构

```
/                       首页 (产品介绍 + 快速开始)
/create                 创作中心 (文字生图 + 图生视频)
/create/text-to-image   文字生图页面
/create/image-to-video  图生视频页面
/gallery                作品画廊 / 灵感广场
/history                我的生成历史
/pricing                定价方案
/login                  登录页
/register               注册页
/settings               个人设置
```

### 6.2 核心页面布局

#### 创作页面 (文字生图)

```
┌──────────────────────────────────────────────────┐
│  Logo    [文字生图] [图生视频]    积分:50   头像   │ ← 顶部导航
├──────────────────────────────────────────────────┤
│                                                  │
│  ┌─────────────────────────────────────────────┐ │
│  │  ✍️ 描述你想生成的图片...                     │ │ ← 提示词输入
│  │                                             │ │
│  │  [风格: 写实 ▼] [尺寸: 1:1 ▼] [模型: Flash ▼]│ │ ← 参数选择
│  │                                    [生成 →]  │ │
│  └─────────────────────────────────────────────┘ │
│                                                  │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐           │
│  │      │ │      │ │      │ │      │           │ ← 生成结果
│  │ 图片1 │ │ 图片2 │ │ 图片3 │ │ 图片4 │           │    网格展示
│  │      │ │      │ │      │ │      │           │
│  │ [下载] │ │ [生成  │ │ [下载] │ │ [下载] │           │
│  │       │ │  视频] │ │       │ │       │           │
│  └──────┘ └──────┘ └──────┘ └──────┘           │
│                                                  │
│  ── 历史生成 ──────────────────────────────────   │
│  [缩略图] [缩略图] [缩略图] [缩略图] [更多 →]     │ ← 历史记录
│                                                  │
└──────────────────────────────────────────────────┘
```

---

## 七、项目目录结构

```
polloai/
├── apps/
│   └── web/                          # Next.js 前端应用
│       ├── app/
│       │   ├── (auth)/               # 认证相关页面
│       │   │   ├── login/
│       │   │   └── register/
│       │   ├── (main)/               # 主应用页面
│       │   │   ├── create/
│       │   │   │   ├── text-to-image/
│       │   │   │   └── image-to-video/
│       │   │   ├── gallery/
│       │   │   ├── history/
│       │   │   └── settings/
│       │   ├── api/                  # API Routes
│       │   │   ├── auth/
│       │   │   ├── generate/
│       │   │   │   ├── image/
│       │   │   │   └── video/
│       │   │   ├── tasks/
│       │   │   ├── upload/
│       │   │   └── user/
│       │   ├── layout.tsx
│       │   └── page.tsx
│       ├── components/
│       │   ├── ui/                   # shadcn/ui 组件
│       │   ├── create/               # 创作页组件
│       │   │   ├── PromptInput.tsx
│       │   │   ├── ModelSelector.tsx
│       │   │   ├── StylePicker.tsx
│       │   │   ├── ImageUploader.tsx
│       │   │   └── ResultGrid.tsx
│       │   ├── layout/               # 布局组件
│       │   │   ├── Header.tsx
│       │   │   ├── Sidebar.tsx
│       │   │   └── Footer.tsx
│       │   └── shared/               # 通用组件
│       │       ├── TaskStatusBadge.tsx
│       │       ├── CreditDisplay.tsx
│       │       └── MediaPlayer.tsx
│       ├── hooks/
│       │   ├── useGenerate.ts
│       │   ├── useTaskPolling.ts
│       │   └── useCredits.ts
│       ├── lib/
│       │   ├── api/                  # API 客户端
│       │   ├── ai/                   # AI 服务封装
│       │   │   ├── nano-banana.ts    # 图片生成
│       │   │   └── veo.ts            # 视频生成
│       │   ├── auth/                 # 认证逻辑
│       │   ├── db/                   # 数据库
│       │   │   ├── prisma.ts
│       │   │   └── schema.prisma
│       │   ├── storage/              # 文件存储
│       │   └── queue/                # 任务队列
│       ├── stores/                   # Zustand 状态
│       ├── types/                    # TypeScript 类型
│       └── utils/                    # 工具函数
├── packages/
│   └── shared/                       # 共享类型/工具 (未来扩展)
├── docker-compose.yml                # 本地开发环境
├── .env.example                      # 环境变量模板
├── package.json
└── turbo.json                        # Turborepo 配置 (可选)
```

---

## 八、核心代码架构

### 8.1 AI 服务适配层

```typescript
// lib/ai/nano-banana.ts - 图片生成服务
import { GoogleGenAI } from "@google/genai";

interface ImageGenerateOptions {
  prompt: string;
  model: "flash" | "pro" | "flash2";
  size?: string;
  count?: number;
  negativePrompt?: string;
}

interface ImageGenerateResult {
  images: { data: Buffer; mimeType: string }[];
}

const MODEL_MAP = {
  flash: "gemini-2.5-flash-image",
  pro: "gemini-3-pro-image-preview",
  flash2: "gemini-3.1-flash-image-preview",
} as const;

export class NanoBananaService {
  private client: GoogleGenAI;

  constructor(apiKey: string) {
    this.client = new GoogleGenAI({ apiKey });
  }

  async generateImage(options: ImageGenerateOptions): Promise<ImageGenerateResult> {
    const model = MODEL_MAP[options.model];
    const response = await this.client.models.generateContent({
      model,
      contents: [options.prompt],
      config: { responseModalities: ["IMAGE"] },
    });

    const images = response.candidates?.[0]?.content?.parts
      ?.filter((part) => part.inlineData)
      .map((part) => ({
        data: Buffer.from(part.inlineData!.data, "base64"),
        mimeType: part.inlineData!.mimeType,
      })) ?? [];

    return { images };
  }
}
```

```typescript
// lib/ai/veo.ts - 视频生成服务
import { GoogleGenAI } from "@google/genai";

interface VideoGenerateOptions {
  prompt: string;
  imageUrl: string;          // 输入图片 URL
  model: "standard" | "fast";
  aspectRatio?: "16:9" | "9:16";
  duration?: 4 | 6 | 8;
  resolution?: "720p" | "1080p";
}

interface VideoGenerateResult {
  operationId: string;       // 异步操作 ID
}

const MODEL_MAP = {
  standard: "veo-3.1-generate-preview",
  fast: "veo-3.1-fast-generate-preview",
} as const;

export class VeoService {
  private client: GoogleGenAI;

  constructor(apiKey: string) {
    this.client = new GoogleGenAI({ apiKey });
  }

  async generateVideo(options: VideoGenerateOptions): Promise<VideoGenerateResult> {
    const model = MODEL_MAP[options.model];
    const image = await this.loadImage(options.imageUrl);

    const operation = await this.client.models.generateVideos({
      model,
      prompt: options.prompt,
      image,
      config: {
        aspectRatio: options.aspectRatio ?? "16:9",
        numberOfVideos: 1,
      },
    });

    return { operationId: operation.name };
  }

  async checkStatus(operationId: string) {
    const operation = await this.client.operations.get({ name: operationId });
    return {
      done: operation.done,
      videos: operation.done
        ? operation.result?.generatedVideos?.map((v) => v.video) ?? []
        : [],
    };
  }

  private async loadImage(url: string) {
    // 从 S3/OSS 下载图片并转换为 API 所需格式
  }
}
```

### 8.2 任务队列处理

```typescript
// lib/queue/video-worker.ts
import { Worker, Queue } from "bullmq";
import { VeoService } from "../ai/veo";
import { db } from "../db/prisma";
import { uploadToStorage } from "../storage";

const videoQueue = new Queue("video-generation", {
  connection: { host: "localhost", port: 6379 },
});

const worker = new Worker("video-generation", async (job) => {
  const { taskId, options } = job.data;

  // 1. 更新任务状态
  await db.generationTask.update({
    where: { id: taskId },
    data: { status: "processing", startedAt: new Date() },
  });

  // 2. 调用 Veo API
  const veo = new VeoService(process.env.GEMINI_API_KEY!);
  const { operationId } = await veo.generateVideo(options);

  // 3. 轮询等待结果
  let result = await veo.checkStatus(operationId);
  while (!result.done) {
    await new Promise((r) => setTimeout(r, 10_000));
    result = await veo.checkStatus(operationId);
  }

  // 4. 上传视频到存储
  const outputUrls = [];
  for (const video of result.videos) {
    const url = await uploadToStorage(video.data, "video/mp4");
    outputUrls.push(url);
  }

  // 5. 更新任务完成
  await db.generationTask.update({
    where: { id: taskId },
    data: {
      status: "completed",
      outputUrls,
      completedAt: new Date(),
    },
  });

  // 6. 通过 WebSocket 通知前端
  await notifyClient(taskId, "completed", outputUrls);
}, {
  connection: { host: "localhost", port: 6379 },
  concurrency: 5,
});
```

---

## 九、开发计划 & 里程碑

### Phase 1: MVP 基础版 (第 1-3 周)

| 周次 | 任务 | 交付物 |
|------|------|--------|
| **W1** | 项目初始化 & 基础设施 | Next.js 项目搭建、数据库设计、环境配置 |
| **W1** | AI API 对接 | Nano Banana & Veo 服务封装、单元测试 |
| **W2** | 文字生图功能 | Prompt 输入、参数选择、图片生成 & 展示 |
| **W2** | 用户认证 | 注册/登录、JWT 鉴权、基本用户信息 |
| **W3** | 图生视频功能 | 图片上传、视频生成、异步任务处理、结果展示 |
| **W3** | 积分系统 | 积分扣减、余额查询、交易记录 |

**Phase 1 交付标准**: 核心生成链路跑通，用户可完成注册→生图→生视频完整流程。

### Phase 2: 体验优化 (第 4-5 周)

| 周次 | 任务 | 交付物 |
|------|------|--------|
| **W4** | 生成历史 & 任务管理 | 历史记录页面、任务状态实时更新 (WebSocket) |
| **W4** | UI/UX 打磨 | 响应式设计、加载动画、错误处理优化 |
| **W5** | 文件管理 | CDN 接入、图片/视频压缩、缩略图生成 |
| **W5** | 性能优化 | API 限流、缓存策略、数据库索引优化 |

### Phase 3: 商业化 (第 6-8 周)

| 周次 | 任务 | 交付物 |
|------|------|--------|
| **W6** | 支付集成 | Stripe 接入、积分充值、订阅方案 |
| **W6** | 风格预设系统 | 预设 Prompt 模板、风格标签 |
| **W7** | 灵感广场 | 公开作品展示、社区发现 |
| **W7** | SEO & 落地页 | 首页优化、产品介绍、SEO 元数据 |
| **W8** | 上线准备 | 监控告警、日志系统、安全审计、压测 |

### Phase 4: 功能扩展 (第 9 周+)

- [ ] 视频延长 (Video Extension) - Veo 3.1 支持最多 20 次延长
- [ ] 首尾帧控制 - 指定视频起始/结束画面
- [ ] 参考图引导 - 角色/风格一致性控制
- [ ] 图片编辑 - 基于对话的 AI 图片修改
- [ ] 批量生成 - 批量任务提交
- [ ] API 开放 - 开发者 API 接口
- [ ] 移动端适配 - PWA 或原生 App

---

## 十、成本估算

### 10.1 API 成本

| 服务 | 单价 | 月预估用量 | 月成本 |
|------|------|-----------|--------|
| Nano Banana Flash | ~$0.039/张 | 10,000 张 | ~$390 |
| Nano Banana Pro | ~$0.134/张 | 2,000 张 | ~$268 |
| Veo 3.1 | ~$0.35/秒 (约$2.8/8秒视频) | 1,000 条 | ~$2,800 |
| **合计** | | | **~$3,458/月** |

> 注: 以上为预估，实际价格以 Google Cloud 控制台为准。Nano Banana Flash 有 500 次/天免费额度。

### 10.2 基础设施成本

| 服务 | 方案 | 月成本 |
|------|------|--------|
| Vercel | Pro | $20 |
| Neon PostgreSQL | Scale | $19 |
| Upstash Redis | Pay as you go | ~$10 |
| Cloudflare R2 | 按用量 | ~$15 |
| Cloudflare CDN | Free/Pro | $0-20 |
| 域名 | .ai 域名 | ~$7/月 |
| **合计** | | **~$71-91/月** |

---

## 十一、风险评估 & 应对

| 风险 | 等级 | 应对策略 |
|------|------|---------|
| API 调用限流/配额不足 | 🔴 高 | 多 API Key 轮询、请求排队、用户提示 |
| AI 生成内容违规 | 🔴 高 | 接入内容安全审核、敏感词过滤、用户举报机制 |
| API 定价变动 | 🟡 中 | 预留模型切换能力、适配层解耦 |
| 视频生成延迟高 | 🟡 中 | WebSocket 实时通知、进度展示、Fast 模型降级 |
| 存储成本增长 | 🟡 中 | 设置文件保留策略、自动清理过期内容 |
| 用户恶意刷量 | 🟡 中 | 限流、验证码、IP 风控 |

---

## 十二、环境变量配置

```env
# .env.example

# ===== Google Gemini API =====
GEMINI_API_KEY=your_gemini_api_key

# ===== Database =====
DATABASE_URL=postgresql://user:pass@host:5432/polloai

# ===== Redis =====
REDIS_URL=redis://localhost:6379

# ===== Storage (Cloudflare R2) =====
R2_ACCOUNT_ID=your_account_id
R2_ACCESS_KEY_ID=your_access_key
R2_SECRET_ACCESS_KEY=your_secret_key
R2_BUCKET_NAME=polloai-assets
R2_PUBLIC_URL=https://cdn.yourdomain.com

# ===== Auth =====
JWT_SECRET=your_jwt_secret
NEXTAUTH_SECRET=your_nextauth_secret
NEXTAUTH_URL=http://localhost:3000

# ===== OAuth =====
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# ===== Stripe (支付) =====
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
```

---

## 十三、参考资源

- [Nano Banana 图片生成文档](https://ai.google.dev/gemini-api/docs/image-generation)
- [Veo 3.1 视频生成文档](https://ai.google.dev/gemini-api/docs/video)
- [Google GenAI Python SDK](https://gist.github.com/patrickloeber/32486cfea158931203e2504261f961e1)
- [Nano Banana Hackathon Kit](https://github.com/google-gemini/nano-banana-hackathon-kit)
- [Pollo.ai 产品参考](https://pollo.ai/zh)
- [Veo 3.1 API 教程](https://www.cursor-ide.com/blog/veo-31-api-tutorial)
- [Nano Banana 2 开发指南](https://www.cursor-ide.com/blog/nano-banana-2-api-tutorial)
- [Veo 3.1 发布公告](https://developers.googleblog.com/introducing-veo-3-1-and-new-creative-capabilities-in-the-gemini-api/)

---

> **文档维护**: 本文档随项目迭代持续更新。
> **最后更新**: 2026-03-09
